let medianOfArrays = function(arr1, arr2) {
    let combined = [...arr1, ...arr2].sort((a, b) => a - b);
    let mid = Math.floor(combined.length / 2);
    return (combined[mid - 1] + combined[mid]) / 2;
};
console.log(medianOfArrays([1, 3, 5], [2, 4, 6])); // Output: 3.5

// IIFE